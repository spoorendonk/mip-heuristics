build*/
.cache/
__pycache__/
bench/results*/
dist/
.venv/
*.pyc
Highs.log

# Local agent/dev tooling — not part of the published artifact.  The gates that
# matter are tracked: .githooks/ (git hooks) and cmake/Lint.cmake (ctest).
.claude/
.codex/

# Release archives are build products of a results tree (18 MB+ of logs);
# `bench/make_archive.py build` regenerates one, and `verify` re-derives its
# tables from the archived logs.  The reader-facing summary is bench/headline/.
bench/archives/
